# Description: A 2D graphics library with support for multiple output devices patched for infinality.
# URL:         http://www.cairographics.org/
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-dropin/issues
# Depends on:  fontconfig xorg-libxext xorg-libxrender xorg-libpixman xorg-xcb-util glib

name=cairo-iu
version=1.14.8
release=2017-01-11 # Using forked 2016.09.09
source=(
	http://cairographics.org/releases/cairo-$version.tar.xz
	https://github.com/archfan/infinality_bundle/archive/2.7.1.zip
)

build() {
	cd cairo-$version

	for p in $SRC/infinality_bundle-2.7.1/03_$name/*.patch; do
		patch -p1 < $p
	done

	./configure --prefix=/usr \
		--enable-xcb=yes \
		--enable-ps \
		--enable-pdf \
		--disable-gtk-doc \
		--enable-tee=yes

	make
	make DESTDIR=$PKG install

	rm -rf $PKG/usr/share
}
