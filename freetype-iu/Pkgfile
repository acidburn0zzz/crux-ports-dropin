# Description: Freetype patched with the infinality patches plus a couple more.
# URL:         https://github.com/bohoomil/fontconfig-ultimate
# Maintainer:  6c37 Team, https://github.com/6c37/crux-ports-dropin/issues
# Depends on:  zlib libpng harfbuzz

name=freetype-iu
version=2.7.1
release=2017-01-11
source=(
	http://download.savannah.gnu.org/releases/freetype/freetype-$version.tar.bz2
	https://github.com/archfan/infinality_bundle/archive/2.7.1.tar.gz
)

build() {
	cd freetype-$version

	for p in $SRC/infinality_bundle-2.7.1/01_freetype2-iu/*.patch; do
		patch -p1 < $p
	done

	./configure --prefix=/usr \
		--with-zlib \
		--disable-nls

	make
	make DESTDIR=$PKG install

	install -Dm755 $SRC/infinality_bundle-2.7.1/01_freetype2-iu/freetype2.sh $PKG/etc/profile.d/freetype.sh
}
